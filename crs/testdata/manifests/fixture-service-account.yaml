# Fixture manifest for the crs/populate.sh contract test. The populate script
# must read a manifest directory like this one and emit a ConfigMap whose data
# embeds the manifest content.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: fixture-sa
  namespace: kube-system
